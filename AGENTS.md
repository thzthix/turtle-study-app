# AGENTS.md

## Goal

거북이와 함께 집중 시간을 버티는 따뜻한 감성의 웹 MVP를 만든다.

사용자는 집중 세션을 시작하고, 거북이는 세션 동안 길을 천천히 걷는다. 세션 중간에는 짧은 돌봄 상호작용이 등장하고, 세션을 완료하면 거북이가 보상 반응을 보여준다.

---

## Current Phase

Phase 1: Web MVP

Focus on:

- 집중 시간 설정과 시작
- 집중 중 거북이 이동 표현
- 중간 돌봄 상호작용
- 세션 종료 보상 화면
- 따뜻한 일러스트형 디자인

Do NOT:

- 백엔드 구축
- 사용자 인증
- 실시간 채팅
- 복잡한 게임 경제
- 과한 설정 페이지
- 네이티브 앱 전환

---

## Scope Rules

- 현재 브랜치의 목적을 벗어나는 기능을 추가하지 않는다.
- 한 번에 하나의 목표만 구현한다.
- 작은 PR 여러 개를 선호한다.
- MVP 검증에 필요하지 않은 기능은 뒤로 미룬다.
- 문서 브랜치와 기능 브랜치를 분리한다.
- 모든 의미 있는 작업은 구현 일기에 남긴다.

---

## Product Rules

- 이 앱의 핵심 가치는 생산성 압박보다 정서적 동행이다.
- 거북이를 계속 보게 만들지 않는다.
- 돌봄 상호작용은 집중을 끊지 않는 짧은 개입이어야 한다.
- 보상은 세션 종료 시점에 가장 크게 느껴져야 한다.
- 경쟁 요소는 압박보다 응원과 가벼운 자극에 가깝게 유지한다.

---

## Design Rules

- 따뜻한 일러스트 톤을 유지한다.
- 기본 흰 배경 위에 보라색 포인트 같은 흔한 톤은 피한다.
- 배경은 평면 단색보다 은은한 그라데이션과 패턴을 활용한다.
- 거북이는 단순하지만 감정이 읽히게 표현한다.
- 애니메이션은 많기보다 의미 있어야 한다.
- 모바일과 데스크톱 모두 자연스럽게 보여야 한다.

---

## Code Rules

- 사람이 빠르게 읽을 수 있는 구조를 최우선으로 한다.
- 각 파일은 한 가지 책임만 갖게 한다.
- 긴 컴포넌트는 작은 UI 조각과 로직으로 분리한다.
- 함수 이름은 역할이 드러나게 작성한다.
- 불필요한 추상화는 만들지 않는다.
- 디버그 코드와 사용하지 않는 코드는 남기지 않는다.
- 주석은 복잡한 의도를 설명할 때만 짧게 사용한다.

---

## Frontend Rules

- TypeScript를 기본으로 사용한다.
- 상태는 가장 단순한 방법부터 시작한다.
- 초기 MVP에서는 전역 상태 라이브러리를 서둘러 넣지 않는다.
- 애니메이션은 CSS 또는 가벼운 프론트엔드 방식으로 우선 구현한다.
- 일러스트는 우선 SVG와 CSS로 제작해 수정 가능성을 높인다.

---

## Branch Rules

- 브랜치는 기능 단위로 만든다.
- 브랜치 이름은 `feat/add-...`, `fix/...`, `chore/...` 형식을 따른다.
- 문서 작업도 별도 목적이면 별도 브랜치로 분리한다.

예시:

- `feat/add-project-guidelines`
- `feat/add-focus-session-ui`
- `feat/add-turtle-animation`

---

## Commit Rules

- 커밋 prefix는 `feat`, `fix`, `refactor`, `chore`, `style`, `docs`를 사용한다.
- 커밋 메시지는 한국어로 작성한다.
- 한 커밋에는 한 가지 목적만 담는다.

예시:

- `docs: 프로젝트 명세 초안 추가`
- `feat: 집중 세션 타이머 화면 추가`
- `style: 거북이 카드 애니메이션 다듬기`

---

## PR Rules

- PR은 하나의 목적만 가져야 한다.
- 변경 이유와 범위를 분명하게 적는다.
- 테스트를 하지 못했다면 이유를 적는다.
- 다음 단계가 남아 있다면 TODO로 남긴다.

---

## Journal Rules

- 각 기능 브랜치 작업에는 구현 일기 기록이 포함되어야 한다.
- 구현 일기에는 구현 내용뿐 아니라 설계 판단도 남긴다.
- 아래 항목을 빠뜨리지 않는다.

필수 기록 항목:

- 작업 목표
- 구현 내용
- 설계 상 고민
- 설계 시 고려한 대안
- 구현 중 고민
- 트러블슈팅
- 트러블슈팅 중 고려한 대안
- 최종 선택과 이유
- 남은 TODO 또는 리스크

- 짧더라도 작업 단위마다 누적 기록한다.
- 성공한 선택뿐 아니라 버린 선택도 기록한다.
- 다음 사람이 읽어도 의사결정 맥락이 보이게 작성한다.

---

## Validation

- 문서는 현재 범위와 비범위를 분명히 설명해야 한다.
- 구현은 문서에 적힌 핵심 루프와 어긋나지 않아야 한다.
- 첫 MVP는 혼자서도 흐름이 이해되는 수준이어야 한다.

---

## Summary

우리는 `귀여운 공부 타이머`를 만드는 것이 아니라, `같이 버티는 거북이 경험`을 만드는 중이다.
